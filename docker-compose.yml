services:
  api:
    build: .
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    restart: always
    volumes:
      - ./jobs.db:/app/jobs.db
    shm_size: '2gb'
